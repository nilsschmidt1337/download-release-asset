name: 'Download Asset'
description: 'Download github.com release asset'

inputs:
  owner:
    required: true
  repo:
    required: true
  tag:
    required: true
  file:
    required: true
  path:
    required: false
  token:
    required: false

runs:
  using: 'node20'
  main: 'dist/index.js'

branding:
  icon: download
  color: blue
